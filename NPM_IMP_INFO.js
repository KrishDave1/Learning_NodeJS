//local dependency - use it only in this particular project
//npm i <packageName>

//global dependency - use it in any project
//npm install -g <packageName>
//sudo npm install -g <packageName> (mac)

// npx is a package runner tool.It does not install the package globally.

//package.json - manifest file (stores important info about project/package)
//package-lock.json - stores exact version of packages that we install so other people working on the project can install the exact same version.

//In terminal type npm init to create package.json file
//npm init -y to create package.json file with default values.